import React from 'react';
import './style/footer.css';

const Footer = ({ title }) => {
  return (
    <footer className="footer">
      <h1>{title}</h1>
    </footer>
  );
};

export default Footer;
