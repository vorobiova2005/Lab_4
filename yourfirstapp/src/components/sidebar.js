import React from 'react';
import './style/sidebar.css';

const SideBar = ({ title }) => {
  return (
    <div class="sidenav">
      <a href="#">+</a>
      <a href="#">+</a>
      <a href="#">+</a>
      <a href="#">+</a>
    </div>
  );
};

export default SideBar;
